{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mb-5"><i class="fa fa-edit"></i> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
