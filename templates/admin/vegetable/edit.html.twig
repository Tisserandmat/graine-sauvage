{% extends '_layout.html.twig' %}
{% block title %}Administration plantes{% endblock %}
{% block body %}
    <div class="container page-content">
        <div class="row justify-content-center">
            <h1>Modification plante</h1>
        </div>


        {{ include('admin/vegetable/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        <div class="row justify-content-between mt-5 "><a class="btn  ml-3 btn-primary-color" href="{{ path('vegetable_index') }}">
                <i
                        class="fas fa-arrow-left"></i> Retour a l'index</a>

            {{ include('admin/vegetable/_delete_form.html.twig') }}</div>
    </div>

{% endblock %}
