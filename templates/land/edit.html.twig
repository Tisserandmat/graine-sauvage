{% extends '_layout.html.twig' %}

{% block title %}Edit Land{% endblock %}

{% block body %}
<div class="container page-content">
  <div class="row justify-content-center">
    <h1>publication terrains</h1>
  </div>
  <div class="row justify-content-center w-100 ">{{ include('land/_form.html.twig', {'button_label': 'Enregistrer'}) }}</div>

  <div class="row justify-content-between mt-5 "><a class="btn  ml-3 btn-primary-color"
                                                    href="{{ path('land_index') }}">Retour a l'index</a>

    {{ include('land/_delete_form.html.twig') }}</div>
</div>
{% endblock %}
